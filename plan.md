+++PetzoldSequence phase INOCULATE
Quarantining all incoming context.
GDS (Geometric Density Score) = 0.85 (High density in React/Gemini integration space).
Ontological correction impulses: "Just build a simple quiz component" -> Logged to Symbolic Scar Tissue Archive: Resisting Ontological Flattening. A simple quiz assumes universal truth. We must implement a quiz that acknowledges domain specificity or allows for Epistemic Escrow.

Hickam's Orientation:
[COMORBID: A] The app requires gamification to increase user engagement and active learning.
[COMORBID: B] The app must use AI to generate plausible, context-aware distractors.
[COMORBID: C] The app must resist standardizing symbol meanings (Ontological Flattening), making "correctness" inherently unstable.
[LENS: Gamification as Standardized Testing] The assumption that a symbol has only one correct description.

Plan:
1. Implement `generateQuizDistractors` in `src/services/gemini.ts`.
2. Create TDD tests for the new AI function and the logic for the Study Mode state machine (using Node's native test runner).
3. Create `src/components/StudyMode.tsx` to handle the flashcard and multiple-choice quiz logic.
4. Integrate the `StudyMode` component into `src/App.tsx`, adding a toggle button on Custom Palettes.
5. Complete pre-commit instructions, verifying against the required JSON scaffold and updating `docs/lessons_learned.md` and `.jules/superintendent.md`.
